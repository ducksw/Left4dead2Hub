<!DOCTYPE html>
<html>
<head>
<title>EDITAR JUGADOR</title>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="container mt-3">
    <a href="/panel/edit" class="bg bg-dark p-2 rounded">VOLVER</a>
    <h2 class="mt-3">Editar a <b>{{user.displayName}}</b></h2>
    <form action="/panel/edit/{{user.steamId}}" method="POST" class="row g-3">

        <div class="col-12 text-center mb-2">
            <img src="{{user.avatar}}" alt="Avatar" class="rounded-circle img-thumbnail" style="width: 150px; height: 150px; object-fit: cover;">
            <p class="mt-2 mb-0 text-muted">{{user.displayName}}</p>
        </div>

        <div class="col-md-6">
            <label class="form-label">Steam ID</label>
            <input type="text" class="form-control" name="steamId" value="{{user.steamId}}" readonly>
        </div>

        <div class="col-md-6">
            <label class="form-label">Display Name</label>
            <input type="text" class="form-control" name="displayName" value="{{user.displayName}}">
        </div>

        <div class="col-12">
            <label class="form-label">Avatar URL</label>
            <input type="text" class="form-control" name="avatar" value="{{user.avatar}}">
        </div>

        <div class="col-12">
            <label class="form-label">Profile URL</label>
            <input type="text" class="form-control" name="profileurl" value="{{user.profileurl}}">
        </div>

        <div class="col-md-4">
            <label class="form-label">Match</label>
            <input type="number" class="form-control" name="match" value="{{user.match}}">
        </div>

        <div class="col-md-4">
            <label class="form-label">Elo</label>
            <input type="number" class="form-control" name="elo" value="{{user.elo}}">
        </div>

        <div class="col-md-4">
            <label class="form-label">Points</label>
            <input type="number" class="form-control" name="points" value="{{user.points}}">
        </div>

        <div class="col-md-4">
            <label class="form-label">Damage</label>
            <input type="number" class="form-control" name="damage" value="{{user.damage}}">
        </div>

        <div class="col-md-4">
            <label class="form-label">Kills</label>
            <input type="number" class="form-control" name="kills" value="{{user.kills}}">
        </div>

        <div class="col-md-4">
            <label class="form-label">Wins</label>
            <input type="number" class="form-control" name="win" value="{{user.win}}">
        </div>

        <div class="col-md-4">
            <label class="form-label">Loser</label>
            <input type="number" class="form-control" name="losser" value="{{user.losser}}">
        </div>

        <div class="col-md-4">
            <label class="form-label">Draws</label>
            <input type="number" class="form-control" name="draw" value="{{user.draw}}">
        </div>

        <div class="col-md-4">
            <label class="form-label">Rank</label>
            <input type="text" class="form-control" name="rank" value="{{user.rank}}">
        </div>

        <div class="col-md-4">
        <label class="form-label">¿Es Admin?</label>
        <select class="form-select" name="isAdmin">
            <option value="false" {{#unless user.isAdmin}}selected{{/unless}}>No</option>
            <option value="true" {{#if user.isAdmin}}selected{{/if}}>Sí</option>
        </select>
        </div>

        <div class="col-12">
        <label class="form-label">Últimos 5 Resultados</label>
        <div class="row">
            {{#each user.lastMatches}}
            <div class="col-md-2">
                <select class="form-select" name="lastMatches">
                <option value="W" {{#if (eq this "W")}}selected{{/if}}>Win</option>
                <option value="L" {{#if (eq this "L")}}selected{{/if}}>Loser</option>
                <option value="D" {{#if (eq this "D")}}selected{{/if}}>Empate</option>
                <option value="N" {{#if (eq this "N")}}selected{{/if}}>None</option>
                </select>
            </div>
            {{/each}}
        </div>
        <div class="col-12 mt-4 text-end">
            <button type="submit" class="btn btn-success">Actualizar</button>
        </div>
</div>

    </form>
</div>
</body>
</html>
